en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            fields:
              unknown: invalid id
              not_array: not array
              array_not_supported: array not supported
